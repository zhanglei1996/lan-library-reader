# LAN Reader 产品与开发计划

> 本文用于规划 LAN Reader 从“局域网文档书架”发展为安全、易用、可配置的本地阅读工具。
> 更新时间：2026-07-27

## 实施状态

`v0.5.0` 已按本文主路线实现：

- `v0.2.0`：TXT、自定义文本类型、复制、下载和配置文件
- `v0.3.0`：语法高亮、二维码、自动刷新和阅读位置
- `v0.4.0`：全文搜索、增量目录缓存、正文索引缓存和实例管理
- `v0.5.0`：临时/固定访问码、会话保护和登录限流

“可以考虑但暂不优先”章节仍是 `v0.5.0` 之后的候选方向，不属于当前版本承诺。

## 1. 产品定位

LAN Reader 的核心体验应保持简单：

```bash
cd 任意资料目录
lan-reader
```

启动后，电脑、手机和平板可以通过局域网浏览文件，但 LAN Reader 不修改源文件。

后续功能遵循以下原则：

1. **零配置可用**：Markdown、PDF、Word、PowerPoint 和 TXT 直接可用。
2. **安全默认值**：代码文件必须由用户主动配置后才进入书架。
3. **始终只读**：不提供上传、编辑、重命名或删除源文件的能力。
4. **移动端优先**：主要操作在手机和平板上也应容易完成。
5. **错误可理解**：扫描失败、文件过大、编码错误等问题必须显示真实原因。
6. **配置可迁移**：配置文件可以随资料目录提交或备份，不依赖某台电脑。

## 2. 当前能力

- 递归建立 Markdown、PDF、Word 和 PowerPoint 书架
- Markdown 阅读、标题目录和前后篇导航
- PDF 浏览器原生预览
- 通过 LibreOffice 转换 Word 和 PowerPoint
- 手机和平板局域网访问
- 文件名搜索
- 深色模式和字号调整
- 服务只读、路径越界防护和隐藏文件保护
- 一条命令停止全部 LAN Reader 实例
- 端口占用后自动寻找下一个端口
- 自动排除常见构建目录
- 支持 `.lan-readerignore`

## 3. v0.2.0 核心目标（已实现）

`v0.2.0：文本阅读与内容操作` 集中完成了以下能力：

1. TXT 和用户指定文本类型的安全预览
2. 一键复制文档内容
3. 一键复制当前文档链接
4. 下载原始文档
5. 项目级配置文件
6. 更完善的错误、文件大小和编码处理

不要在同一版本同时加入编辑、全文搜索、账号系统和在线同步，以免破坏当前轻量、只读的定位。

## 4. 功能设计

### 4.1 TXT 与自定义文本文件预览

#### 默认支持

`.txt` 应成为内置文档类型，不需要配置。

TXT 阅读器第一版提供：

- 等宽字体
- 自动换行开关
- 字号调整
- 行号开关
- 复制全文
- 下载原文件
- 上一篇和下一篇
- UTF-8、UTF-8 BOM 和 GB18030/GBK 中文文本识别

#### 用户配置其他扩展名

启动目录可以创建 `.lan-reader.json`：

```json
{
  "version": 1,
  "title": "Normandy 后端资料",
  "textPreview": {
    "extensions": [
      ".log",
      ".json",
      ".yaml",
      ".yml",
      ".js",
      ".ts",
      ".java",
      ".sql"
    ],
    "maxBytes": 8388608,
    "lineNumbers": true,
    "wrap": true
  },
  "features": {
    "copy": true,
    "download": true
  }
}
```

规则：

- 扩展名统一转换为小写，并要求以 `.` 开头
- 重复扩展名自动去重
- 配置的文件始终按纯文本读取，绝不执行 JavaScript、HTML 或其他代码
- 不允许配置覆盖 Markdown、PDF 和 Office 的内置处理方式
- `maxBytes` 设置上下限，例如 1 KB 至 32 MB
- 未知配置项给出警告，但不应导致服务崩溃
- 配置格式错误时明确指出文件名和错误位置
- 修改配置后点击“刷新书架”即可生效，尽量不要求重启服务

#### 为什么代码文件不默认展示

代码仓库可能包含接口地址、密钥样例、内部逻辑和客户信息。LAN Reader 在局域网中没有默认身份认证，因此 `.js`、`.ts`、`.java`、`.sql` 等类型应由用户主动加入，不能默认暴露整个源码仓库。

#### 文本安全边界

- 服务端先检查大小，再读取内容
- 使用二进制特征检测，疑似二进制文件拒绝文本预览
- 文本通过 React 普通文本节点渲染，禁止使用 `dangerouslySetInnerHTML`
- HTML、SVG、JavaScript 即使被配置，也只显示源码
- 保留现有路径越界、隐藏文件和符号链接限制
- 文件在扫描后被删除时返回“文件已不存在”，页面允许刷新

### 4.2 一键复制

阅读工具需要区分两类复制：

#### 复制内容

- Markdown：复制 Markdown 原文
- TXT/代码：复制完整原文
- PDF/Office：第一阶段不做全文提取，避免引入质量不稳定的解析
- 复制成功后显示短暂提示，例如“已复制全文”
- 超大文本复制前显示大小提示，避免移动端卡顿

后续可以给 Markdown 增加“复制纯文本”，但不应替代“复制源码”。

#### 复制链接

复制当前局域网地址，并保留 `?doc=` 文档定位参数。用户可直接发给同一 Wi-Fi 下的其他设备。

需要注意：局域网地址通常是普通 HTTP，部分移动浏览器不允许 HTTP 页面直接调用现代 Clipboard API。因此实现必须提供降级方案：

1. 优先使用 `navigator.clipboard.writeText`
2. 不可用时使用隐藏文本框和 `document.execCommand("copy")`
3. 仍失败时打开可手动长按复制的文本框

这样不能只在 `localhost` 测试，必须使用真实局域网 IP 在手机浏览器验证。

#### 代码块复制

Markdown 中每个代码块增加独立的“复制”按钮。这是高价值、低风险功能，可以和复制全文同时进入 v0.2.0。

### 4.3 下载原文档

所有进入书架的文件都应显示“下载原文件”：

- Markdown、TXT、代码：下载未修改的源文件
- PDF：下载原 PDF
- Word/PowerPoint：下载原 Office 文件，而不是转换后的 PDF

服务端设计：

```text
GET /api/file?path=<相对路径>&download=1
HEAD /api/file?path=<相对路径>&download=1
```

当 `download=1` 时返回：

```text
Content-Disposition: attachment
X-Content-Type-Options: nosniff
Cache-Control: no-store
```

验收重点：

- 中文文件名在 Safari、Chrome 和 Edge 中正确
- 支持大文件流式下载，不把整个文件读入内存
- 下载仍经过书架根目录、隐藏文件和符号链接校验
- 文件不存在、无权限或下载中途被删除时能正确结束连接

### 4.4 阅读工具栏

建议把当前工具栏整理为：

```text
[文件类型] 文件名        [复制内容] [复制链接] [下载] [上一篇] [下一篇]
```

移动端空间不足时收纳到“更多”菜单，但“下载”和“复制”最多两次点击完成。

不同类型的动作矩阵：

| 文件类型 | 复制内容 | 复制链接 | 下载原文件 | 预览 |
| --- | --- | --- | --- | --- |
| Markdown | 支持 | 支持 | 支持 | 支持 |
| TXT/自定义文本 | 支持 | 支持 | 支持 | 支持 |
| PDF | 暂不提取全文 | 支持 | 支持 | 支持 |
| Word/PowerPoint | 暂不提取全文 | 支持 | 支持 | 依赖 LibreOffice |

## 5. 配置系统设计

### 5.1 配置文件职责

- `.lan-reader.json`：书架名称、文本类型、阅读功能和显示偏好
- `.lan-readerignore`：不需要扫描的目录或路径

两者职责分开，避免一个 JSON 文件承载很长的排除清单。

### 5.2 配置加载顺序

```text
内置默认值 → .lan-reader.json → 命令行参数
```

命令行参数优先级最高。第一阶段不必把每个配置都做成命令行参数，只保留 `root`、`host` 和 `port`。

### 5.3 配置校验

实现独立的配置模块，负责：

- 文件读取
- JSON 解析
- 版本检查
- 默认值合并
- 类型和范围校验
- 生成面向用户的中文错误
- 向前端返回经过清理的公开配置

不要把整个配置文件直接返回浏览器。未来若加入密码或访问令牌，服务端配置与公开配置必须严格分离。

### 5.4 配置版本

配置必须包含：

```json
{
  "version": 1
}
```

遇到未来版本时应提示“当前 LAN Reader 版本无法识别此配置”，避免静默使用错误设置。

## 6. API 与数据模型调整

### 6.1 文件类型

前端 `DocumentKind` 增加：

```ts
type DocumentKind =
  | "markdown"
  | "text"
  | "pdf"
  | "word"
  | "powerpoint";
```

文本节点增加可选字段：

```ts
interface FileNode {
  type: "file";
  name: string;
  path: string;
  kind: DocumentKind;
  language?: string;
}
```

### 6.2 文本读取接口

新增统一文本接口：

```text
GET /api/text?path=<相对路径>
```

响应：

```json
{
  "name": "server/index.mjs",
  "path": "server/index.mjs",
  "content": "...",
  "encoding": "utf-8",
  "language": "javascript",
  "size": 15700,
  "modifiedAt": "2026-07-27T10:00:00.000Z"
}
```

为了兼容旧客户端，`/api/markdown` 继续保留，并与 `/api/text` 共用底层安全文本读取函数。

### 6.3 书架快照

现有 `/api/snapshot` 继续作为首页唯一入口，并增加公开配置：

```json
{
  "tree": [],
  "info": {
    "name": "资料",
    "documentCount": 10,
    "scan": {},
    "capabilities": {},
    "preferences": {
      "copy": true,
      "download": true,
      "textWrap": true,
      "lineNumbers": true
    }
  }
}
```

## 7. 后续高价值功能

### v0.3.0：阅读体验与分享（已实现）

#### 语法高亮

- 对用户配置的代码扩展名显示语法高亮
- 使用按需加载语言包，避免首屏包体积过大
- 高亮失败时回退到纯文本
- 大文件自动关闭高亮，只保留纯文本和行号

#### 当前书架二维码

- 启动页面和终端提供局域网地址二维码
- 手机扫码即可打开
- 多网卡时允许用户选择正确地址

#### 自动刷新

- 监听目录变化并刷新目录树
- 对短时间内大量文件变化做防抖
- 忽略构建目录和 `.lan-readerignore` 中的路径
- 文件监听不可用时保留手动刷新

#### 阅读位置

- 在浏览器本地保存最近阅读文档和滚动位置
- 数据只保存在当前设备，不写入资料目录
- 文件移动或删除后自动清理失效记录

### v0.4.0：检索与大型书架（已实现）

#### 全文搜索

- 第一阶段只索引 Markdown、TXT 和配置的文本类型
- 搜索结果显示文件名、命中片段和所在目录
- 索引在内存中生成，不写入用户目录
- 文件数量过多时分批索引并显示进度
- 支持取消和重新构建索引

#### 增量扫描与缓存

- 缓存目录树和文件元数据
- 只重新扫描发生变化的目录
- 避免十万级目录每次刷新都完整遍历
- 缓存放在系统临时目录，不污染源目录

#### 运行实例列表

```bash
lan-reader list
```

显示：

- 书架名称
- 启动目录
- 端口
- 本机地址
- 进程状态
- 启动时间

可进一步支持：

```bash
lan-reader stop 8081
lan-reader stop --root /path/to/books
```

### v0.5.0：可选访问保护（已实现）

如果开始预览源码、内部文档或工作资料，应增加可选保护：

- 启动时生成临时访问码
- 支持通过环境变量设置访问码
- 登录 Cookie 设置 `HttpOnly`、`SameSite=Strict`
- 防止暴力尝试
- 保留完全关闭认证的家庭局域网模式

不建议把明文密码写进 `.lan-reader.json`，也不建议在命令行参数中传递密码，因为命令行可能被系统进程列表看到。

## 8. 可以考虑但暂不优先

以下功能有价值，但应在核心文本阅读稳定后再做：

- EPUB 电子书预览
- 图片画廊模式
- Markdown 打印与导出 PDF
- 文档收藏和标签
- 多书架聚合首页
- PWA 安装
- PDF 文本提取
- Office 文本提取

PWA 离线缓存默认不应缓存文档正文，否则工作资料可能残留在手机浏览器中。

## 9. 明确不做的事情

- 不执行被预览的 JavaScript、HTML、Shell 或其他代码
- 不提供服务器端脚本插件
- 不允许配置任意外部转换命令
- 不默认暴露隐藏文件
- 不跟随符号链接访问书架外部
- 不在没有明确设计前加入编辑、上传和删除
- 不把局域网服务宣传为可直接暴露到公网

## 10. v0.2.0 实施顺序

### 阶段 A：配置与扫描

1. 新建配置读取和校验模块
2. `.txt` 加入默认支持类型
3. 将用户配置扩展名加入目录扫描
4. 为文本文件生成 `language` 信息
5. 快照接口返回公开阅读偏好
6. 配置变更后刷新生效

### 阶段 B：安全文本接口

1. 抽取通用文本读取函数
2. 增加大小限制
3. 增加 UTF-8 BOM 和 GB18030/GBK 解码
4. 增加二进制文件识别
5. 新增 `/api/text`
6. 保持 `/api/markdown` 兼容

### 阶段 C：阅读界面

1. 新建 TextReader
2. 增加自动换行和行号
3. 增加复制全文
4. Markdown 代码块增加复制按钮
5. 增加复制当前链接
6. 增加复制失败的手动降级界面

### 阶段 D：下载

1. `/api/file` 支持 `download=1`
2. 所有文档类型增加下载入口
3. 校验中文文件名和大文件流式传输
4. 校验 HEAD、Range 和中断连接行为

### 阶段 E：文档与发布

1. README 增加 `.lan-reader.json` 示例
2. README 增加复制和下载说明
3. 增加升级说明
4. 更新演示书架
5. 完成 npm 包内容检查
6. 发布 `v0.2.0`

## 11. 测试矩阵

### 服务端

- 默认识别 `.txt`
- 大小写扩展名正确处理
- 用户扩展名去重
- 非法扩展名和非法配置被拒绝
- 未来配置版本被拒绝
- UTF-8、BOM、GB18030/GBK 文本正确读取
- 二进制内容伪装成 `.txt` 时拒绝预览
- 超大文本返回 413
- 路径穿越、隐藏文件和符号链接仍被阻止
- 下载使用 attachment
- 中文下载文件名正确编码
- 大文件保持流式传输
- 配置刷新不会触发重复并发扫描

### 前端

- Markdown 和文本复制成功提示
- Clipboard API 不可用时进入降级流程
- 复制链接保留文档路径
- TXT 换行和行号设置生效
- 超长单行文本不会撑破页面
- 错误状态不显示“空书架”
- 下载按钮对所有文档类型可用
- 手机窄屏工具栏可操作
- 代码内容中的 HTML 和脚本不会执行

### 回归

- Markdown 标题目录和跳转
- PDF Range 请求
- Office 转换和原文件下载
- 端口自动递增
- 一键停止全部实例
- `.lan-readerignore`
- `normandy-backend` 大型目录扫描
- npm 全局安装后的 `lan-reader` 命令

## 12. v0.2.0 完成标准

只有同时满足以下条件才进入发布：

- 用户在普通目录执行 `lan-reader` 能直接阅读 TXT
- 用户通过 `.lan-reader.json` 添加 `.js` 后，刷新即可预览 JavaScript 源码
- JavaScript 源码只显示、不执行
- Markdown 和文本可以复制全文
- 所有书架文档可以下载原文件
- 手机通过局域网 HTTP 可以复制，或得到清晰的手动复制降级
- 中文 TXT 和中文文件名正常
- 大文件、二进制文件和非法配置都有明确错误
- 现有 Markdown、PDF、Office、端口和停止命令全部通过回归
- README、配置示例和发布说明完整

## 13. 推荐版本路线

| 版本 | 目标 | 主要内容 |
| --- | --- | --- |
| `0.1.3` | 扫描稳定性 | 构建目录排除、自定义忽略、端口递增、错误反馈 |
| `0.2.0` | 文本与内容操作 | TXT、自定义文本类型、复制、下载、配置文件 |
| `0.3.0` | 阅读体验 | 语法高亮、二维码、自动刷新、阅读位置 |
| `0.4.0` | 大型书架 | 全文搜索、增量索引、实例列表 |
| `0.5.0` | 可选保护 | 局域网访问码和会话保护 |

这条路线优先解决日常使用频率最高的问题，同时避免一次引入过多依赖和安全风险。
